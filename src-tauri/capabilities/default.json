{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main terminal window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
